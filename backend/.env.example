# MongoDB
MONGO_URL=mongodb://localhost:27017
DB_NAME=mul_salary

# Comma-separated exact frontend origins
CORS_ORIGINS=http://localhost:3000

# Required for administrative endpoints; generate a long random value
ADMIN_API_KEY=replace-with-a-long-random-value
